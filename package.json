{
  "name": "jekamobile",
  "version": "1.0.0",
  "description": "Instafix Mobile Repairs - Full Stack App",
  "scripts": {
    "build": "npm install --prefix server && npm install --include=dev --prefix client && npm run build --prefix client",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
